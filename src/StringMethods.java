
public class StringMethods {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		// Array of chars, reference data types have useful methods
		
		String name = "Rodrigo";
		
		// that method is key sensitive 
		// boolean result = name.equals("Rodrigo");
		// boolean result = name.equalsIgnoreCase("Rodrigo");
		
		// return the amount of chars on the String
		// int result = name.length();
		
		// return the char on the index that is passed on the argument
		// char result = name.charAt(0);
		// return the index of the char that is passed on argument on the String
		// int result = name.indexOf("R");
		// returns if the String is empty
		// boolean result = name.isEmpty();
		// takes the string and transform every char in his upper Case equivalent
		// String result = name.toUpperCase();
		// takes the string and transform every char in his upper Lower equivalent
		// String result = name.toLowerCase();
		// Takes out all empty spaces inside the String 
		// String result = name.trim();
		// Replaces specific chars 
		// String result = name.replace(oldChar, newChar);
		// String result = name.replace('l', 'a');
		
		// System.out.println(result);
		
	}

}
