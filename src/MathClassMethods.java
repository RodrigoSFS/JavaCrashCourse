
public class MathClassMethods {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		double x = 3.14;
		double y = -10;
		
		//double z = Math.min(x, y);
		double z = Math.abs(y); // absolute
		double square = Math.sqrt(z); // square
		z = Math.ceil(x); // round up a number
		z = Math.ceil(x); // round down a number
		
		System.out.println(z);
	}

}
